# Same-origin /api proxy is the default. Leave NEXT_PUBLIC_API_URL unset.
SHOPAWARE_BACKEND_URL=http://127.0.0.1:8000
# For HTTPS/remote deployments, set NEXT_PUBLIC_WS_URL to the secured WS endpoint.
